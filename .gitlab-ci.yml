--- 
build: 
  script: 
    - rm -rf output
    - pelican content
  stage: build
  tags: 
    - ci
    - shell
    - ssh

test:
  script:
    - cd output;nohup python -m pelican.server &
  stage: test
  tags:
    - ci
    - shell
    - ssh
  when: manual

stages: 
  - build
  - test
